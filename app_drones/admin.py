"""Project-level admin is intentionally empty.

User admin is registered in user_management.admin so that Django's
autodiscovery picks it up (app_drones is the project package, not an
installed app).
"""
